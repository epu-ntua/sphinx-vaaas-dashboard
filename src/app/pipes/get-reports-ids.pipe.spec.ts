import { GetReportsIDsPipe } from './get-reports-ids.pipe';

describe('GetReportsIDsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetReportsIDsPipe();
    expect(pipe).toBeTruthy();
  });
});
