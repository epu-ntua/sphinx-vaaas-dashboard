import { ReportOutPipe } from './report-out.pipe';

describe('ReportOutPipe', () => {
  it('create an instance', () => {
    const pipe = new ReportOutPipe();
    expect(pipe).toBeTruthy();
  });
});
