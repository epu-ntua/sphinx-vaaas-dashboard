import { GetReportPipe } from './get-report.pipe';

describe('GetReportPipe', () => {
  it('create an instance', () => {
    const pipe = new GetReportPipe();
    expect(pipe).toBeTruthy();
  });
});
